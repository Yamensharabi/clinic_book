//------------------------------------class for define a the static images that we used in our app
class AppSvgs {
  static const root = 'assets/svgs/'; //--the root of images folder path
  static const cameraIcon = '${root}camera_icon.svg';
  static const closeIcon = '${root}close.svg';
  static const cameraDialog = '${root}camera_dialog.svg';
  static const galleryIcon = '${root}gallery_icon.svg';
  static const person = '${root}person.svg';
}
